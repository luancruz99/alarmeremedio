import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
   mainContainer: {
      flex: 1,
      alignItems: 'center',
   },
   heading: {
      padding: 20,
      alignItems: 'center'
   },
   nextAlarmText: {
      fontSize: 18,
      color: '#000',
   },
   nextAlarmTime: {
      fontSize: 50,
      color: '#000',
   },
   nextAlarmLabel: {
      fontSize: 20,
      color: '#000',
   },
   listAlarmsArea: {
      marginTop: 15,
   },
})

export { style };